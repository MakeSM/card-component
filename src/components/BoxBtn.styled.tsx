import { styled } from 'styled-components';

export const BoxBtn = styled.div`
   display: flex;
   position: absolute;
   inset: 298px 96px auto 20px;
   gap: 12px;
   button {
      cursor: pointer;
   }
`;
